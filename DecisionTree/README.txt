To run the files, run the main script "Testing.m" which will call the function ID3 which will call subsequent functions found in this folder. The return of the tree does not work as intended but since i can debug and walk through the program, I was able to form trees and carry out the expierement. I shot myself in the foot not using python....
